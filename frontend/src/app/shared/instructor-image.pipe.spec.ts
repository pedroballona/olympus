import { InstructorImagePipe } from './instructor-image.pipe';

describe('InstructorImagePipe', () => {
  it('create an instance', () => {
    const pipe = new InstructorImagePipe();
    expect(pipe).toBeTruthy();
  });
});
